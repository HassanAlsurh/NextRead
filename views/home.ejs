<%- include("./partials/nav.ejs") %>

<main>
    <h1>NextRead</h1>

    <% if (user) { %>
        <p>Welcome, <%= user.username %>.</p>
    <% } else { %>
        <p>Nothing here yet</p>
    <% } %>

    <% if (user) { %>
        <a href="/auth/<%= user.id %>">auth edit page</a>
        <% } %>
        <a href="/books/new">new book page</a>
</main>
</body>
</html>
